node_modules
.claude
build

# configurazione locale per lo sviluppo (non committare)
.env.local
.env.development.local
.env.test.local
.env.production.local

# contesto locale per Claude (non condiviso)
CLAUDE.md
